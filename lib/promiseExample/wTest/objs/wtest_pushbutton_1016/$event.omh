On evClick ## Event Parameters - pRow( Itemreference )
	Do iResults.$define("")
	Do $cinst.$redraw()
	
	Do $cinst.$sendHTTPRequest("A") Returns lPromise
	Do lPromise.$then($cinst().$_onResponseX.$ref,row("B")).$then($cinst().$_onResponseX.$ref,row("C")).$then($cinst().$_onResponseX.$ref)