
If pResults.httpStatusCode=200
	Do iResults.$add(utf8tochar(pResults.responseContent))
	Do $cinst.$redraw()
	Do $cinst.$sendHTTPRequest(pExtraParams.C1).$then($cinst().$_onResponseB.$ref,row("C"))
Else
	Breakpoint Error!
End If