# Calculate luid as OW3.$makeuuid(kFalse)
Calculate lPromise as $objects.oPromise.$newref($cinst.$objref(),pTag)
Do iPromises.$add(lPromise)

# Send to trace log Created promise. There are now [iPromises.$linecount] promises.

Quit method lPromise