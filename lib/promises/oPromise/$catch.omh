Set reference iRejectCallback to pCallback.$ref
Calculate iExtraRejectParams as pExtraParams

# If the promise is already rejected, call the catch function immediately:
If state=kPromiseState.REJECTED
	Do $cinst.$reject(iPendingResults,iPendingErrText)
End If